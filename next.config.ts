import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // @ts-ignore
  allowedDevOrigins: ['vegetative-rosalee-nonlive.ngrok-free.dev', 'localhost:3000'],
};

export default nextConfig;
